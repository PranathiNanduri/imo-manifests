kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: imo
nodes:
- role: control-plane
  kubeadmConfigPatches:
  - |
    kind: ClusterConfiguration
    apiServer:
      extraArgs:
        enable-admission-plugins: NodeRestriction
# Optional: bump containerd memory swap defaults etc. (usually not needed)
